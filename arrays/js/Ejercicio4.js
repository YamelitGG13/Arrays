function invertirArreglo(arr) {
    let invertido = [];
    for (let i = arr.length - 1; i >= 0; i--) {
    ;
}

let miArreglo = [1, 2, 3, 4, 5];
onsole.log(invertirArreglo(miArreglo));   invertido.push(arr[i]);
    }
    return invertido