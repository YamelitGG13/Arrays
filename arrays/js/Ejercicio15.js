function buscarIndice(arr, valor) {
    return arr.indexOf(valor);
  }
  
  let miArreglo = [10, 20, 30, 40, 50];
  console.log(buscarIndice(miArreglo, 30)); 
  console.log(buscarIndice(miArreglo, 60)); 
  